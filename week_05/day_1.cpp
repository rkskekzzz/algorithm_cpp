#include <iostream>

double result;
double x, y, c;
void input() {
	std::cin >> x >> y >> c;
}

double calculate(double mid){
	return (x * x - y * y)
}

void solution() {
	double start;
	double mid;
	double end;

	while (start <= end) {
		mid = (start + mid) / 2;
		if ()
	}
}

void preset() {
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(NULL);
	std::cout.tie(NULL);
}

void output() { std::cout << result; }

int main() {
	preset();
	input();
	solution();
	output();
	return 0;
}
